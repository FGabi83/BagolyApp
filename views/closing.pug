extends layout

include mixins/_head
include mixins/_head-end

block head 
  +head()
  +headEnd()

block content
  main.main__vertical
    .closing--wrapper 
      h1= title
      div
        p.text__dark <span class="text__bold"> Leadó:</span> #{actualIncome} Ft
        p.text__dark <span class="text__bold">Borravaló:</span> #{tipsNum} Ft
        p.text__dark <span class="text__bold">Napi váltó:</span> #{openingAmountNum} Ft  