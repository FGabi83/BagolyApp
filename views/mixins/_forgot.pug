mixin forgotForm()
  form.form(action="/account/forgot" method="POST")
    h2.subtitle Elfelejtettem a jelszavam
    label(for="email") Email
    input.form__input(type="email" name="email")
    input.btn.btn-primary(type="submit" value="Új jelszót kérek →")