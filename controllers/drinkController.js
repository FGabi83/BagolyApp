const mongoose = require('mongoose');

exports.getDrinks = async (req, res) => {
  //need a model for the drinks
  // Query the database for a list of all drinks per category
  // render the drinks template with the drinks data   
}; 